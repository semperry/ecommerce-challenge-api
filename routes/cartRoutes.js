const express = require("express");

const ShoppingCartSchema = require("../models/cartModel");
const ProductSchema = require("../models/productModel");
const Router = express.Router();

module.exports = Router;
